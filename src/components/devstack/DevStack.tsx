import React from 'react';

const dev_stack = ({devPromise}) => {
    console.log(devPromise)
const devStack = use(devPromise)
    console.log(devStack,"devStack")    
    return (
        <div>
            
        </div>
    );
};

export default dev_stack;